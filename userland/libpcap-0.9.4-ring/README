
Make sure that when you build the libpcap library, you modify the libpcap Makefile as follows:
1. Add " -I ../../kernel/include -I ../libpfring" to the INCLS variable
2. Add "../libpfring/pfring.o" to the LIBS variable
